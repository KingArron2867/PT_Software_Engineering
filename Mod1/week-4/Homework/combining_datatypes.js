// Combining data types

// 1.
const crayonBox = {
    'colors':['red', 'blue', 'purple', 'magenta', 'brown']
 };
 console.log(crayonBox.colors[2]);

 // 2.
 const bottle = {
    cap: {
        material: 'metal',
        color: 'white'

    }
}   
        console.log(bottle.cap.color);

// 3. 
const receipt = [
    {name: 'Breifs', price: 3.50},
    {name: 'Boxers', price: 2.50},
    {name: 'Socks', price: 1.50}
]
console.log(receipt[0].price)

// 4. 
const apartmentBuilding = [['Yosef', 'Angel', 'Jose', 'Kobe', 'Nipsey']
];
        console.log(apartmentBuilding[0][2]);




////////////// Combine objects more than one 

// 1. 
const knit = () => {

    const cover = {

       item: 'scarf',
       size: '6ft'
    }
    
       console.log(cover.item)
    }
    knit();


// 2.
const cSelector
    const crayonBox = {
        'colors':['red', 'blue', 'purple', 'magenta', 'brown']
    };
    console.log(crayonBox.colors[2]);
    cSelector();
    console.log(cSelector([2]));

// 3. 
  
const powerButton = function() {
    // options = function() {
         console.log("select a song");
     
}; 
   
powerButton();




///// Modle Vending Machine.

// 1.

function add(x, y) {
    x=1; 
    y=2
    return (console.log(x+y))
}
add();


// 2. 

function subtract(x, y) {
    x=4;
    y=2
    return (console.log(x-y))
}
subtract();

// 3.

function multiply(x, y) {
    x=4;
    y=2
    return (console.log(x*y))
}
multiply()


// 4.

function divide (x, y) {
    x=4;
    y=2
    return (console.log(x / y))
}
divide()
console.log('')

// 5.

function calculate (n1,n2,operate) {
    function operate() {
        n1
        n2
        this.number = n1*n2
        return console.log(this.number)
    }
// 6.

function operates () {
    return ("You've been calculated")
} 
}

// 7. 

calculate(01,50)
calculate(02,44)
calculate(83,24)
calculate(22,80)



////// Funcition def placement
// Error reading

const bar = () => {
    console.log('bar here');
}
bar();

const foo = () => {
    console.log('foo here');
}
foo();

// Error reading

"error has already been defined."


